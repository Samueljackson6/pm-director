# pm-director 项目移交文档

> 生成: 2026-07-02 | 更新: 2026-07-02 (Session 2)
> 移交自: Sisyphus 工作会话

---

## 一、项目总览

| 属性 | 内容 |
|------|------|
| **项目** | 合同管理系统 (Contract Management System) |
| **GitHub** | `github.com/Samueljackson6/pm-director` (master) |
| **236 生产** | `192.168.0.236` - 运行 FastAPI + SQLite + Vben |
| **本地路径** | `D:\Tare-workspace\pm-director\` |
| **后端** | Python FastAPI (uvicorn, 端口 8800) |
| **数据库** | SQLite (`database/project_management.db`, 294KB) |
| **前端** | Vben Admin v5 自定义页面 (Ant Design Vue) |
| **开发环境** | Docker (docker-compose) |

---

## 二、代码结构

```
D:\Tare-workspace\pm-director\
├── backend/
│   ├── main.py              ← FastAPI 后端主文件 (283行, 11244B)
│   └── requirements.txt     ← 依赖: fastapi, uvicorn
├── database/
│   └── project_management.db ← SQLite 数据库 (294KB)
├── cache/contracts/         ← 56个合同标准数据 JSON 文件 (从236同步)
├── ui/                      ← Vben 自定义前端代码（从236拉取）
│   ├── src/api/
│   │   ├── contracts.ts     ← 合同 API
│   │   ├── invoices.ts      ← 发票 API
│   │   ├── suppliers.ts     ← 供应商 API
│   │   ├── request.ts       ← requestClient 封装 (token刷新/错误处理)
│   │   └── index.ts         ← API 导出汇总
│   ├── src/views/
│   │   ├── contracts/index.vue    ← 合同列表页
│   │   ├── contracts/detail.vue   ← 合同详情页
│   │   ├── invoices/index.vue     ← 发票列表页
│   │   └── suppliers/index.vue    ← 供应商列表页
│   ├── src/router/routes/modules/
│   │   ├── contracts.ts     ← 合同路由 (ignoreAccess: true)
│   │   ├── invoices.ts      ← 发票路由 (ignoreAccess: true)
│   │   └── suppliers.ts     ← 供应商路由 (ignoreAccess: true)
│   ├── .env                 ← 开发环境变量
│   ├── .env.development     ← 开发环境配置
│   └── .env.production      ← 生产环境配置 (API→192.168.0.236:8800)
├── ui-dashboard/            ← 简易 Vue3 仪表盘（旧版，保留）
├── tools/                   ← 工具脚本 (合同提取、识别、流水线)
│   ├── batch_contract_workflow.py
│   ├── contract_cache_manager.py
│   ├── contract_extractor_v3.py
│   ├── contract_intelligent_recognizer.py
│   ├── contract_pipeline.py
│   └── pp_structure_recognize.py
├── scripts/                 ← 辅助脚本
├── docs/                    ← 项目文档 (51份)
├── Dockerfile               ← Python 3.11-slim
├── docker-compose.yml       ← 后端服务 (端口8800, 挂载database/ + cache/)
└── WORKFLOW.md              ← 开发工作流文档
```

> ⚠️ **注意**: WORKFLOW.md 部分内容已过时（引用了已移除的 `vben/` 目录），新会话启动后建议更新。

---

## 三、关键架构决策

| 决策 | 说明 |
|------|------|
| **Vben 框架分离** | Vben Admin v5 是独立项目（npm create 获取），不是 pm-director 子目录。pm-director 只包含 UI 自定义代码 (`ui/`) |
| **SQLite 只读** | 后端使用 SQLite 只读 API，数据通过外部工具写入 |
| **ignoreAccess** | 所有自定义路由均设 `ignoreAccess: true`，绕过 Vben 登录认证 |
| **Vben 响应格式** | 后端包装 `{code: 0, data: {...}}` 以兼容 Vben requestClient |
| **Docker 测试** | 使用 Docker 容器跑测试，用完即停。`docker compose up -d` / `docker compose down` |
| **RuoYi 分离** | RuoYi Office Vben (端口 5666) 是独立项目，与 pm-director 无关 |

---

## 四、部署与服务器

### 236 生产服务器

| 属性 | 内容 |
|------|------|
| **地址** | `192.168.0.236` |
| **用户** | samuel |
| **SSH 配置** | Host `236` (IdentityFile: `~/.ssh/id_ed25519_236`) |
| **工作目录** | `/home/samuel/.openclaw/workspace/pm-director/` |
| **后端进程** | FastAPI 端口 8800（uvicorn，systemd 或 nohup 管理） |
| **数据库** | `database/project_management.db` (294KB) |
| **缓存数据** | `cache/contracts/` (56 个 JSON) |
| **仪表盘** | 简易 Vue3 仪表盘 (ui-dashboard, 端口 5888) |

### 236 与 GitHub 差异

236 上的 git 仓库落后 GitHub 几个提交（未包含 Docker 配置和 ui/ 自定义文件），不影响生产运行。

### 236 已知目录

```
236:/home/samuel/.openclaw/workspace/pm-director/
├── backend/main.py          ← 同本地版本 (11244B)
├── database/*.db            ← 生产数据库
├── cache/contracts/         ← 合同 JSON 缓存
├── tools/                   ← 工具脚本
├── scripts/                 ← 辅助脚本
└── ui-vben/                 ← Vben 完整项目 (含框架源码，约1729个包)
    └── apps/web-antd/src/
        ├── api/             ← 自定义 API (已同步到 GitHub ui/src/api/)
        ├── views/           ← 自定义页面 (已同步到 GitHub ui/src/views/)
        └── router/routes/modules/  ← 自定义路由 (已同步到 GitHub)
```

---

## 五、Git 提交历史

```
f7318d1 chore: remove temp files (server.json, vite.err)
fd4bff4 feat(docker): add dev environment with FastAPI backend
a3714c9 feat(ui): add Vben custom pages (contracts/invoices/suppliers) from 236
b5ac706 chore: clean up Vben from repo, finalize migration structure
1a7e471 docs: add dev workflow document + update backend format
924adb4 fix(id-system): ZH02 primary key, SGSC secondary reference
90465f4 fix(P0): demo progress on 5 contracts, expand ID mapping by fuzzy match
6d51239 feat(P0): stage-payment links, project progress API, invoice tax
d7c5e47 chore: add server.json with proxy and service config
94bface feat: contract management system v1
```

---

## 六、RuoYi Office Vben 目录状态

**路径**: `D:\Tare-workspace\RuoYi Office Vben fSamuel\`

已清理干净，仅保留原始项目文件：

| 类型 | 内容 |
|------|------|
| 项目文档(14个) | PROJECT.md, CLAUDE.md, ARCHITECTURE.md, STANDARDS.md 等 |
| 原始目录 | `.codegraph/`, `.omo/`, `docs/` |
| **已清理** | 162 个临时脚本 + 6 个临时目录 + node_modules + antd 原型页 + 会话数据 |

---

## 七、启动与使用

### Docker 测试环境

```bash
cd D:\Tare-workspace\pm-director
docker compose up -d          # 启动 → http://localhost:8800
# ... 测试验证后 ...
docker compose down            # 关闭释放资源
```

### 本地开发 (Windows 原生 Python)

```bash
cd D:\Tare-workspace\pm-director
pip install -r backend/requirements.txt
uvicorn backend.main:app --host 0.0.0.0 --port 8800 --reload
```

### 236 部署

```bash
ssh 236
cd /home/samuel/.openclaw/workspace/pm-director
python3 -m uvicorn backend.main:app --host 0.0.0.0 --port 8800
```

---

## 八、已完成工作（2026-07-02 Session）

| # | 任务 | 状态 | 说明 |
|---|------|------|------|
| 1 | **OCR数据源变革** | ✅ | 创建 `docs/合同识别数据源说明-20260702.md`，更新MEMORY.md、综合说明、OCR架构方案、流程设计文档 |
| 2 | **WORKFLOW.md重写** | ✅ | 移除过时vben/引用，增加Docker/原生Python/Vben独立项目等工作流 |
| 3 | **stage↔payment关联分析** | ✅ | 确认38条链接已覆盖所有既有stage又有payment的项目 |
| 4 | **Docker环境验证** | ✅ | FastAPI后端localhost:8800响应正常（30条合同、API全部可用） |
| 5 | **Vben前端部署确认** | ✅ | 236上dist已构建（7月1日20:21），contracts/detail/invoices/suppliers bundle已编译，前端5777正常响应 |

## 九、当前待办 / 下一步建议

| # | 事项 | 优先级 | 说明 |
|---|------|--------|------|
| 1 | **合同JSON数据入库** | 🔴高 | 56个标准数据JSON中约26个未导入DB，需人工校验后执行 `scripts/import_to_db.py` |
| 2 | **合同编号补全** | 🟡中 | 9个合同缺少合同编号，需从JSON/飞书对照补全（编号完整度76.9%→目标100%） |
| 3 | **236 PDF原件目录整理** | 🟡中 | 按 `/home/samuel/OCdoc/合同PDF原件/` 整理，供系统展示用 |
| 4 | **Docker Desktop修复** | 🟢低 | 本地Docker服务未启动（com.docker.service stopped），影响容器化测试 |
| 5 | **Vben API路径配置** | 🟢低 | `ui/.env.production` 中 `VITE_GLOB_API_URL` 指向236，若本地开发需调整 |

---

*移交完成。新会话先从本文档和 `docs/合同识别数据源说明-20260702.md` 获取最新上下文。*
