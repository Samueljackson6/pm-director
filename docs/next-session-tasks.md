# 4 项大改造任务清单（下一会话）

> 前置依赖：数据治理已完成（42 合同审计通过，阶段/付款/服务字段已清洗）。
> 详见 `docs/data-cleanup-report-2026-07-12.md`

---

## 任务1: 合同详情页 UI 重设计 (EPBT v8 风格)
**现状**: 当前 detail.vue 布局基本但简陋，服务内容/阶段/条款等卡片未做差异化设计
**目标**: 参考 EPBT v8 公告详情页的风格——轻重结合、信息密度适中、视觉层次分明

**具体要求**:
- 科研类和技术服务类合同使用**不同的详情页模板**（两类合同字段结构不同）
- 服务类：服务内容/方式/地点/期限/进度/质量要求 → 6 个独立卡片或信息块
- 科研类：阶段详情+经费预算 → 专用布局
- 所有可折叠区块默认折叠
- 颜色/字体/间距规范化（不需要炫技，但要专业、干净、现代）
- 服务内容 >150 字时显示展开/收起按钮（已有）
- 响应式适配

---

## 任务2: 技术服务类合同——服务内容分字段展示
**现状**: contracts 表已有 `service_content` / `service_method` / `service_location` / `service_period` / `service_schedule` / `service_quality` 六字段
**目标**: 详情页独立展示这 6 个字段，不再只显示一个大文本块

**具体要求**:
- 6 个字段各一个信息行/卡片
- 前端需读取 `service_schedule` 和 `service_quality`（新增字段）
- 编辑功能也要覆盖这 6 个字段
- 如果某字段为空，不显示该行

---

## 任务3: 编辑功能覆盖度完善
**现状**: 当前编辑只能改合同基本信息和部分字段，不覆盖阶段/付款/服务字段
**目标**: 

**A. 合同信息编辑**:
- 基本字段（合同名称/金额/日期/甲乙方等）——已有，确认完整性
- 服务字段（6 个）——**新增**
- 付款计划（新增/编辑/删除）——**已有基础，确认完整**

**B. 科研类专用编辑**:
- 阶段内容（remarks）——**新增**
- 阶段考核目标（acceptance_criteria）——**需确认**（可能已有）
- 每个阶段的编辑字段需有清晰的标签（"阶段内容" / "考核目标"）
- 经费预算编辑

**C. 按合同类型区分编辑字段**:
- 科研类：阶段+预算专用编辑器
- 服务类：服务字段+付款专用编辑器

---

## 任务4: 项目团队卡片数据价值提升
**现状**: personnel 表 34 人，project_personnel 表 101 行关联
**目标**: 团队卡片不止显示"姓名+电话"，增加角色/职责/工时等维度

**具体要求**:
- 从项目人员表中提取更多角色信息（如有）
- 区分：项目负责人 / 甲方联系人 / 乙方联系人 / 项目成员
- 颜色/图标区分不同角色
- 如果没有额外数据，至少让已有信息呈现得更清晰

---

## 附加改进（如果时间允许）
- A. 所有页面（合同列表/项目/供应商/发票详情）的 UI 统一美化
- B. 合同列表搜索/筛选体验优化（已有基础，确认无 bug）
- C. ZH02-202508013 的 service_content 从 PDF 重提取（需 OCR 管道，复杂度高，可降级）
- D. ZH02-202512058 / ZH02-202601010 的"联系人信息行"从前端过滤（避免显示为空白阶段）
