# 交接前 Git 环境清理记录（2026-07-20）

> **目的：** 在核心业务整改正式交接前，清点并清理可安全删除的本地工作树、历史分支和过期本地引用；保留所有未验证安全的工作成果。  
> **执行范围：** `D:\Tare-workspace\pm-director` 本地 Git 仓库。  
> **执行原则：** 先证据、后删除；不删除当前工作树、未提交文件、未合并分支、stash 或不可达恢复对象。

---

## 1. 执行前基线

- 当前工作树：`D:\Tare-workspace\pm-director`
- 当前分支：`codex/phase3-handoff`
- 当前工作树数量：1
- 远端主线：`origin/master`
- 当前工作树中已有未提交文档及其他脚本文件；本次不删除、不覆盖、不暂存这些文件。

执行前的本地分支：

| 分支 | 提交 | 相对 `origin/master` | 初始结论 |
|---|---:|---|---|
| `claude/elegant-hopper-73cb11` | `19498f7` | 已合并，ahead 0 | 可删除 |
| `claude/vigilant-williams-b511c0` | `19498f7` | 已合并，ahead 0 | 可删除 |
| `docs/process-improvement-record` | `f93fa1f` | squash 前原始提交 | 需验证后删除 |
| `feature/phase2-foreign-key-enable` | `58d56f3` | 未合并，ahead 1 | 必须保留 |
| `codex/phase3-handoff` | `d984759` | PR #45 已合并；当前工作树正在使用 | 必须保留 |
| `master` | `3df3b33` | 本地落后 `origin/master` 8 个提交 | 安全快进 |

---

## 2. 只读核验结果

### 2.1 工作树

`git worktree list --porcelain` 仅返回当前工作树，不存在其他可移除工作树，也不存在 `git worktree prune --dry-run` 可清理的失效元数据。

### 2.2 GitHub PR 对照

| 本地分支 | PR/状态 | 结论 |
|---|---|---|
| `codex/phase3-handoff` | PR #45，2026-07-20 已合并 | 当前分支，保留至文档交接提交完成或显式切换。 |
| `docs/process-improvement-record` | PR #35，2026-07-13 已合并 | 本地原始提交被 squash 合并，进一步验证补丁等价。 |
| `claude/elegant-hopper-73cb11` | 无独立 PR；提交已在主线祖先中 | 可安全删除。 |
| `claude/vigilant-williams-b511c0` | 无独立 PR；与上一分支指向同一提交且已在主线祖先中 | 可安全删除。 |
| `feature/phase2-foreign-key-enable` | 无 PR；仍有独有提交 | 不可删除。 |

### 2.3 Squash 等价验证

对 `docs/process-improvement-record` 与已合并的 PR #35 主线提交 `2028489` 执行了两项验证：

- `git patch-id --stable` 结果一致：`0715ff9e983bd1892a593e3ad32776b6d8aa6cfb`；
- 两个提交的 tree object 一致。

因此，该分支没有独有文件内容，可作为已被 PR #35 完整吸收的历史分支删除。

### 2.4 保留分支的风险说明

`feature/phase2-foreign-key-enable` 的唯一提交 `58d56f3` 包含：

- `backend/database.py` 中启用外键的改动；
- `scripts/check-data-integrity.sh` 与 Docker 完整性检查脚本；
- 阶段 2 数据契约/任务文档。

该提交未进入 `origin/master`，且与当前整改 Epic 的 [#48 数据真值、金额单位与关系契约](https://github.com/Samueljackson6/pm-director/issues/48) 直接相关。为防止误删数据治理候选方案，本次明确保留，后续由 #48 决定：

1. 从中选择可复用内容并以新的受控 PR 落地；或
2. 确认其假设已过时后，再按审批记录删除。

---

## 3. 已执行清理

### 3.1 已删除本地分支

| 分支 | 删除方式 | 删除依据 |
|---|---|---|
| `claude/elegant-hopper-73cb11` | `git branch -d` | 已是 `origin/master` 祖先，无独有提交。 |
| `claude/vigilant-williams-b511c0` | `git branch -d` | 已是 `origin/master` 祖先，与上一分支指向同一提交。 |
| `docs/process-improvement-record` | `git branch -D` | PR #35 已 squash 合并，补丁 ID 与 tree object 均等价，且未被任何工作树使用。 |

> 对第三个分支使用强制删除并非跳过安全检查：普通 `git branch -d` 因 squash 合并无法识别祖先关系而拒绝删除；强制删除前已完成补丁与 tree 双重等价验证。

### 3.2 已同步本地引用

- 执行 `git fetch --prune origin`；
- 将本地 `master` 快进至 `origin/master` 的 `b231318`；
- 执行 `git worktree prune`，未发现或删除任何有效工作树；
- **未执行** `git gc --prune`，保留 stash 与不可达对象作为恢复缓冲。

---

## 4. 清理后状态

### 4.1 工作树

| 项目 | 结果 |
|---|---|
| 工作树数量 | 1 |
| 保留工作树 | `D:\Tare-workspace\pm-director` |
| 当前分支 | `codex/phase3-handoff` |
| 失效 worktree 元数据 | 0 |

### 4.2 本地分支

| 分支 | 状态 | 处理 |
|---|---|---|
| `master` | 已与 `origin/master` 对齐 | 保留 |
| `codex/phase3-handoff` | 当前工作树分支；PR #45 已合并 | 保留，待本次文档工作明确提交/切换后再处理 |
| `feature/phase2-foreign-key-enable` | 未合并，保留 1 个数据完整性相关独有提交 | 保留，交由 #48 审核 |

### 4.3 未提交文件边界

清理后当前工作树仍包含未提交文件。它们不是本次“其他工作树/分支”清理的安全删除对象：

- 本轮新增/修改的整改主计划与交接文档；
- 已存在的 `scripts/batch-query-*`、`scripts/query-by-credit-code.js`、`scripts/qcc_credit_codes.json` 等 QCC 查询相关文件。

处理要求：

- 整改主计划与交接文档应由后续文档提交单独提交；
- QCC 脚本必须由其创建者或独立 Issue 评审决定保留、提交或删除；
- 在它们被分类前，禁止使用 `git clean -fd`、递归删除或覆盖式重置工作树。

---

## 5. 交接前最终结论

1. 不存在其他本地工作树需要清理；
2. 3 个可证实安全的历史分支已删除；
3. 本地 `master` 已与远端主线一致；
4. 唯一未合并分支 `feature/phase2-foreign-key-enable` 已明确保留原因和负责人入口（#48）；
5. 当前环境已达到“工作树/分支结构清楚、无已验证可删遗留分支、未提交文件不被误删”的交接前安全状态；
6. 在文档提交和 #48 分支决策完成前，不应宣称整个 Git 工作区“零未提交变更”。

---

## 6. 后续动作

- [ ] 将本轮整改主计划、交接文档、环境清理记录以独立文档 PR/提交落库；
- [ ] 在 #48 审核 `feature/phase2-foreign-key-enable` 中的外键与完整性脚本；
- [ ] 对 QCC 未跟踪脚本建立独立 Issue 或确认归属后处理；
- [ ] 当前分支的文档变更合并后，切换到更新后的 `master` 或建立新的整改工作分支；
- [ ] 在正式移交时附带本记录、#46 Epic、交接文档和最终 Git 状态。

---

## 修订记录

| 日期 | 版本 | 说明 |
|---|---|---|
| 2026-07-20 | v1.0 | 首次完成工作树、分支、PR、合并关系和安全清理核验。 |