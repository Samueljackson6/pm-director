# PM Director 审计整改执行报告（第一、二波）

> 执行日期：2026-07-11  
> 执行方式：低成本代理分工实现，主代理审查、集成、构建和回归  
> 数据原则：所有确定性数据修复均先备份、dry-run、应用、再验证

## 一、已完成整改

### 1. 非法发票文本编码

- 新增安全修复工具：`scripts/repair_invoice_text_encoding.py`。
- 默认只 dry-run，只有传入 `--apply` 才写数据库。
- 新增 4 个 unittest，覆盖：
  - dry-run 不修改数据库；
  - GBK BLOB 转 Unicode TEXT；
  - UTF-8 BLOB 转 Unicode TEXT；
  - 正常 SQLite TEXT 不被误判。
- 已在备份后修复 `invoice_id=72/73` 的 8 个异常文本字段。
- 修复后 dry-run 结果为 0 条异常。

### 2. 合同详情 500

- 修复阶段 `start_time=NULL` 进入正则表达式导致的 TypeError。
- 合同不存在时补充数据库连接关闭。
- 新增 2 个回归测试。
- `SGSCLSOOSDJS250072`、`ZH02-202604023` 均恢复 HTTP 200。

### 3. 综合看板 500

- 清理非法发票编码后，`/api/dashboard/overview` 恢复 HTTP 200。

### 4. 供应商详情

- 统一为 Vben `{code, data, message}` 响应 envelope。
- 供应商不存在时补充连接关闭。
- 新增 2 个 API 契约测试。
- 供应商详情页已恢复正常展示。

### 5. 发票金额单位

- 新增 `yuanToWan()` 纯函数和 Vitest。
- 发票金额、税额、价税合计统一执行“元 / 10000 = 万元”。
- 发票 313 从错误的 `31076.50 万元` 修复为 `3.11 万元`。

### 6. 项目列表 VxeGrid

- 将不存在的 `gridApi.on()` 改为 Vben 规范的 `gridEvents.cellDblclick`。
- 将不存在的 `commitProxyQuery()` 改为 `gridApi.query()`。
- 新增回归契约测试。
- Playwright 已确认不再出现 `gridApi.on is not a function`。

### 7. Readiness 与 CORS

- 新增 `/ready`，验证数据库连接并在 finally 中关闭。
- 数据库异常时固定返回 503，不泄露内部异常信息。
- Docker healthcheck 从 `/health` 改为 `/ready`。
- CORS 从 `*` 收敛为 `PM_CORS_ORIGINS` 环境变量白名单。
- 默认允许本地 `18090/8900/5777`，未列来源被拒绝。

### 8. CI 和测试环境

- 安装项目已声明但本地 venv 缺失的 `python-multipart`。
- 测试 SQLite schema 补齐 `payments.planned_date`。
- CI 中 `ruff check backend/ --quiet || true` 改为强制失败门禁。
- 当前 `ruff check backend` 通过。

### 9. 本项目页面类型错误

已清理以下文件的全部当前 TypeScript 错误：

- `contracts/detail.vue`
- `contracts/components/stage-gantt.vue`
- `projects/detail.vue`
- `api/projects.ts`
- `projects/index.vue`
- `invoices/detail.vue`

全仓 TypeScript 错误由 399 降至 356；剩余错误属于仓库其他模块，未擅自扩大整改范围。

## 二、验证结果

| 验证项 | 结果 |
|---|---|
| 后端 pytest | 15 passed |
| 前端 Vitest | 5 files / 9 tests passed |
| Ruff | passed |
| Python 编译检查 | passed |
| 前端生产构建 | passed，约 4 分钟 |
| 本项目目标文件 TypeScript 错误 | 0 |
| 全仓 TypeScript | 仍有 356 个既有错误 |
| Docker 后端状态 | healthy，使用 `/ready` |
| 综合看板 API | 200 |
| 两个原 500 合同 API | 200 |
| 供应商详情 | 正常 |
| 发票金额 | 正确显示 3.11 万元 |
| 项目列表运行时错误 | 已消失 |
| 42 合同回归 | 42/42 核心得分 ≥ 8，无 HTTP 500 |

## 三、备份和回滚

整改前数据库备份：

`database/backups/project_management-before-remediation-20260711-140258.db`

若需要回滚数据修复，应先停止后端容器，再使用该备份恢复；不得在后端运行时直接覆盖 SQLite 文件。

## 四、仍需业务确认的整改

以下事项没有足够事实依据，未自动修改：

1. 16 组重复 SGSC 映射的 canonical ID。
2. 付款计划单位异常、重复记录及合同金额不一致。
3. 74 条阶段时间不完整数据的补齐规则。
4. 真实认证方案：RuoYi 统一认证、独立 JWT 或企业 SSO。
5. 全仓其他 356 个 TypeScript 错误是否纳入本项目整改范围。

详细确认清单：`docs/审计整改-需业务确认项-20260711.md`。

## 五、已知非阻断项

- VxeTable 仍有 `row-config.height` 废弃警告和部分参数警告。
- 前端构建主 chunk 约 3.6MB，存在拆包优化空间。
- 42 合同中部分合同仍没有付款区块，这是数据缺失/ID 口径问题，不是页面阻断。
- 当前真实认证尚未落地，生产发布前必须完成认证方案确认。
